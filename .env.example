# HeyGen LiveAvatar API
# Server-side only - NOT exposed to browser
HEYGEN_API_KEY=your_heygen_api_key_here

# OpenAI Realtime API
# ⚠️ WARNING: Phase 5 uses NEXT_PUBLIC_ prefix for client-side access
# This exposes the API key in the browser bundle! NOT suitable for production.
# Phase 6 will implement secure server-side token generation.
NEXT_PUBLIC_OPENAI_API_KEY=your_openai_api_key_here
